# Default owners for everything in this repo, unless a later match takes precedence.
# Confirm @PeerbitsSolution/interop-team is a real GitHub team before publishing —
# replace with the actual team/usernames if it isn't.

*                           @PeerbitsSolution/interop-team

# Security-critical paths always require review from the owning team,
# even if a contributor has broader merge rights.
/src/token*                 @PeerbitsSolution/interop-team
/src/**/auth*               @PeerbitsSolution/interop-team
SECURITY.md                 @PeerbitsSolution/interop-team
